#include <iostream>
using namespace std;

int main() {
    int sum=0;
    int positive_integer=0;
    while (positive_integer>=0)
    {cout << "Enter positive integer" << endl;
    cin >> positive_integer;
    if (positive_integer>=0)
    {sum = sum+positive_integer;}
        else {sum = sum;}
    }


cout << "The total sum of all positive integers you input is " << sum << endl;}